#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv)
{

	char data[100];

	printf("Enter x and y (no spaces, comma-separated): \n");
	/*
	char message[] = "Def Leppa,rd rocks!";

	int i = 0;
	int length = strnlen(message, sizeof(message));

	while(message[i] != ',' && i < length)
	{

		i++;
	}
	if (i < length)
	{
		printf("Comma is located at %d\n", i);
	}
	else
	{
		printf("Comma not found\n");
	}
	*/
}